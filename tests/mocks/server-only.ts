/**
 * Test shim for the `server-only` marker so pure server modules can be
 * imported under vitest (Node). In the Next.js app this marker enforces
 * server-only usage at build time.
 */
export {};
