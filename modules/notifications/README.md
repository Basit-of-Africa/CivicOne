# Notifications module

Phase 1 scope: placeholder only (`/notifications`).

## Future scope

- In-app notifications for application milestones
- Email / SMS delivery (behind `server/email.ts` abstraction)
- Preference management
