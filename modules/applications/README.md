# Applications module

Phase 4 scope: a generic, configuration-driven application engine. Schema,
migration, reference generator and status labels live here; the service
engine, actions and UI are added by the Phase 4 tasks that follow.

Phase 1 scope: placeholder only (`/applications`).

## Future scope

- Service application lifecycle (draft → submitted → processing → outcome)
- Application forms powered by server-side Zod schemas
- Attachments and document uploads
- Status tracking and notifications
